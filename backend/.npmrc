# Skip all install lifecycle scripts (including @prisma/client postinstall).
# That postinstall runs prisma generate into node_modules/.prisma and often hits
# EPERM on Windows / OneDrive when renaming query_engine-windows.dll.node.
# After npm install, run: npm run generate
ignore-scripts=true
